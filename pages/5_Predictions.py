import streamlit as st
from PIL import Image

st.title("📈 Прогнози моделей")

st.markdown(
    """
Графіки показують, наскільки добре моделі відтворюють фактичні продажі.  
Головний акцент — чи модель здатна прогнозувати **високі пікові значення**,  
і наскільки щільно її передбачення відповідають ідеальній лінії `y = x`.
"""
)

# ===============================================================
# CATBOOST
# ===============================================================
st.subheader("📌 CatBoost (Tuned)")

st.markdown(
    """
CatBoost показує найкращий результат серед тестованих моделей:

- точки щільно розташовані вздовж лінії `y = x`;
- модель добре відтворює як середні, так і високі значення продажів;
- похибки на піках значно менші, ніж у XGBoost та RandomForest.

Це робить CatBoost найстабільнішою моделлю для практичного застосування.
"""
)

st.image(Image.open("plots/actual_vs_predicted_catboost.png"), use_container_width=True)

# ===============================================================
# XGBOOST
# ===============================================================
st.subheader("📌 XGBoost (Base)")

st.markdown(
    """
XGBoost добре відтворює загальний тренд, але:

- має більший розкид залишків, особливо при продажах > 15 000;
- частіше **недооцінює пікові значення**, що видно з точок нижче червоної лінії;
- працює гірше за CatBoost, але все ще дає прийнятний результат.

Підходить для базових прогнозів, але не оптимальний для піків.
"""
)

st.image(Image.open("plots/actual_vs_predicted_xgboost.png"), use_container_width=True)

# ===============================================================
# RANDOM FOREST
# ===============================================================
st.subheader("📌 RandomForest")

st.markdown(
    """
RandomForest демонструє найслабший результат:

- широке “хмарне” розсіювання точок;
- суттєві похибки при високих значеннях продажів;
- модель стабільна для низьких продажів, але неточна для денних піків;
- прогнозована лінія помітно відхиляється від ідеальної `y = x`.

Модель може бути використана як базова, але поступається бустинговим алгоритмам.
"""
)

st.image(
    Image.open("plots/actual_vs_predicted_randomforest.png"), use_container_width=True
)
