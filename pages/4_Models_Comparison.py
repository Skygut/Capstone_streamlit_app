import streamlit as st
import pandas as pd

st.title("🤖 Порівняння моделей")

st.markdown(
    """
У цьому розділі наведено порівняння результатів всіх моделей.
Метрики оцінено у реальних одиницях продажів (євро),  
що робить інтерпретацію простою і практично корисною.
"""
)

metrics = pd.read_csv("data/metrics.csv")

st.subheader("📋 Таблиця метрик моделей")
st.markdown("Порівнюються MAE, RMSE, WAPE, MAPE та R².")
st.dataframe(metrics, use_container_width=True)

st.markdown(
    """
---

# 🏆 Основні висновки

- **Найкраща модель за всіма ключовими метриками (MAE, RMSE, WAPE, MAPE, R²):**  
  **📌 Stacked Ensemble (GPU base + CPU meta)**  
  Це найточніша модель серед усіх протестованих.

- **Найкраща модель за співвідношенням точність / швидкість:**  
  **📌 CatBoost (Tuned)**  
  Точність майже така ж, як у Stacked Ensemble,  
  але час виконання — у десятки разів менший.

- **RandomForest (Base/Tuned)**  
  показує хороші результати у нижньому діапазоні продажів,  
  але поступається моделям бустингу на високих значеннях.

- **XGBoost (Base)**  
  добре відтворює тренд, але має вищі помилки порівняно з CatBoost та ансамблями.

- **Weighted Ensemble (GPU)**  
  демонструє гірші результати, ніж інші ансамблеві моделі,  
  і тому не є оптимальним варіантом для фінального застосування.

---

### 📌 Підсумок:
Для промислового використання рекомендується  
**Stacked Ensemble** (максимальна точність)  
або **CatBoost (Tuned)** (швидкість + стабільність + інтерпретованість).
"""
)
